# Erlaubt Installation wenn Peer-Dependencies nicht exakt matchen
# Nötig weil @ai-sdk/openai zod@^3 deklariert, das Projekt aber zod@4 nutzt
# (Die Pakete sind trotzdem kompatibel - nur die Peer-Dep-Deklaration ist veraltet)
legacy-peer-deps=true
